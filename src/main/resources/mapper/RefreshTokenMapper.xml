<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yf.yuanfen.user.mapper.RefreshTokenMapper">

    <resultMap id="RefreshTokenResultMap" type="com.yf.yuanfen.user.entity.RefreshToken">
        <id     column="id"         property="id"/>
        <result column="user_id"    property="userId"/>
        <result column="token"      property="token"/>
        <result column="expires_at" property="expiresAt"/>
        <result column="revoked"    property="revoked"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO refresh_tokens (user_id, token, expires_at, revoked)
        VALUES (#{userId}, #{token}, #{expiresAt}, #{revoked})
    </insert>

    <select id="selectByToken" resultMap="RefreshTokenResultMap">
        SELECT id, user_id, token, expires_at, revoked, created_at
        FROM refresh_tokens
        WHERE token = #{token}
        LIMIT 1
    </select>

    <delete id="deleteByUserId">
        DELETE FROM refresh_tokens WHERE user_id = #{userId}
    </delete>

    <update id="revokeByToken">
        UPDATE refresh_tokens SET revoked = 1 WHERE token = #{token}
    </update>

</mapper>
