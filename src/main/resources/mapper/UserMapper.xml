<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yf.yuanfen.user.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.yf.yuanfen.user.entity.User">
        <id     column="id"            property="id"/>
        <result column="username"      property="username"/>
        <result column="email"         property="email"/>
        <result column="phone"         property="phone"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="created_at"    property="createdAt"/>
        <result column="updated_at"    property="updatedAt"/>
        <result column="nickname"      property="nickname"/>
        <result column="gender"        property="gender"/>
        <result column="birth_date"    property="birthDate"/>
        <result column="city"          property="city"/>
        <result column="address"       property="address"/>
        <result column="avatar_url"    property="avatarUrl"/>
        <result column="last_login_at" property="lastLoginAt"/>
    </resultMap>

    <select id="selectByPhone" resultMap="UserResultMap">
        SELECT id, username, email, phone, password_hash, created_at, updated_at
        FROM users
        WHERE phone = #{phone}
        LIMIT 1
    </select>

    <select id="selectByEmail" resultMap="UserResultMap">
        SELECT id, username, email, phone, password_hash, created_at, updated_at
        FROM users
        WHERE email = #{email}
        LIMIT 1
    </select>

    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT id, username, email, phone, password_hash, created_at, updated_at
        FROM users
        WHERE LOWER(username) = LOWER(#{username})
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, phone, password_hash)
        VALUES (#{username}, #{email}, #{phone}, #{passwordHash})
    </insert>

    <select id="existsByPhone" resultType="boolean">
        SELECT COUNT(1) > 0 FROM users WHERE phone = #{phone}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(1) > 0 FROM users WHERE email = #{email}
    </select>

    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(1) > 0 FROM users WHERE LOWER(username) = LOWER(#{username})
    </select>

    <select id="selectProfileById" resultMap="UserResultMap">
        SELECT id, nickname, gender, birth_date, city, address, avatar_url
        FROM users
        WHERE id = #{id}
    </select>

    <update id="updateProfile">
        UPDATE users
        <set>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="birthDate != null">birth_date = #{birthDate},</if>
            <if test="city != null">city = #{city},</if>
            <if test="address != null">address = #{address},</if>
            <if test="avatarUrl != null">avatar_url = #{avatarUrl},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateAvatarUrl">
        UPDATE users SET avatar_url = #{avatarUrl} WHERE id = #{id}
    </update>

    <resultMap id="PublicUserResultMap" type="com.yf.yuanfen.user.entity.User">
        <id     column="id"          property="id"/>
        <result column="nickname"    property="nickname"/>
        <result column="avatar_url"  property="avatarUrl"/>
        <result column="city"        property="city"/>
        <result column="birth_date"  property="birthDate"/>
        <result column="last_login_at" property="lastLoginAt"/>
    </resultMap>

    <select id="listUsers" resultMap="PublicUserResultMap">
        SELECT id, nickname, avatar_url, city, birth_date, last_login_at
        FROM users
        ORDER BY last_login_at DESC, id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countUsers" resultType="int">
        SELECT COUNT(1) FROM users
    </select>

    <update id="updateLastLoginAt">
        UPDATE users SET last_login_at = NOW() WHERE id = #{id}
    </update>

</mapper>
